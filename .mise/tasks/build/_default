#!/usr/bin/env bash
set -e

#MISE quiet=true
#MISE alias='build:debug'
#MISE description="debug build orpheus"
#MISE sources=['Cargo.lock', 'src/orpheus/Cargo.toml', 'src/orpheus/src/**/*.rs']
#MISE outputs=['target/debug/orpheus']

cargo build -p orpheus
